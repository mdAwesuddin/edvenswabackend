'use strict';

/**
 * preferential-issue router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::preferential-issue.preferential-issue');
